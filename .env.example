# Copier ce fichier en .env et adapter les valeurs (le .env n'est jamais commité)

# Base de données locale (WAMP/XAMPP)
DB_HOSTNAME=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=vip_school

# Base de données distante (production)
DB_REMOTE_HOSTNAME=localhost
DB_REMOTE_PORT=3306
DB_REMOTE_USERNAME=utilisateur
DB_REMOTE_PASSWORD=mot_de_passe
DB_REMOTE_DATABASE=base_de_donnees
